from django.conf.urls import url
from user_auth.views import *

urlpatterns = [
    url(r'^phone/login/', phone_login),
    url(r'^phone/login/', phone_login),
    url(r'^phone/login/', phone_login),
    url(r'^phone/login/', phone_login),
]